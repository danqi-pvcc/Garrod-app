// app/page.tsx

export default function Home() {
  return (
    <main style={{ padding: "40px", fontFamily: "system-ui, sans-serif" }}>
      <h1>Garrod App 已成功部署 🎉</h1>
      <p>你现在看到的是根路径 / 的首页。</p>
      <p>后面我们可以从这里跳转到 /health 页面。</p>
    </main>
  );
}
